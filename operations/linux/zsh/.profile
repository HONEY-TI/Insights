# =========================================================
# PROFILE
# =========================================================

export PATH="$HOME/.local/bin:$PATH"

if [ -n "$DISPLAY" ]; then
    export DISPLAY="$DISPLAY"
fi

export XAUTHORITY="$HOME/.Xauthority"
